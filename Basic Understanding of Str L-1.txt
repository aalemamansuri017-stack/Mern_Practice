//practice Basic Understanding of String Levle-1  

let str = "Aalema Mansuri"

 // 1. Write a program to find the length of a string.  
 console.log(str.length);

// 2. Convert a string to uppercase using toUpperCase.*/
console.log(str.toUpperCase());

// 3. Convert a string to lowercase using toLowerCase(). 
console.log(str.toLowerCase());

// 4. Print the first character of a string.
console.log(str[0]);

//5. Print the last character of a string
console.log(str.at(-1));

output:
14
AALEMA MANSURI
aalema mansuri
A
i
